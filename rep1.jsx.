function App() {
  return (
    <div className="min-h-screen w-full bg-[#173626] flex flex-col items-center justify-center text-white font-sans">
      <h1 className="text-4xl font-bold mb-4 flex items-center gap-3">
        🚜 TractoRent
      </h1>
      <p className="text-xl text-gray-300 mb-8">Plateforme de location de tracteurs</p>
      
      <div className="flex flex-col items-center gap-2">
        <div className="w-8 h-8 border-4 border-white border-t-transparent rounded-full animate-spin"></div>
        <p className="mt-4 text-sm opacity-60">Chargement de la plateforme...</p>
      </div>
    </div>
  );
}

const rootElement = document.getElementById('root');
const root = ReactDOM.createRoot(rootElement);
root.render(<App />);
